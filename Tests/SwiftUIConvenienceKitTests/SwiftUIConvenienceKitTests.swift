import XCTest
@testable import SwiftUIConvenienceKit

final class SwiftUIConvenienceKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIConvenienceKit().text, "Hello, World!")
    }
}
